# Security profile
model = "gpt-5.6"
model_provider = "openai"
model_reasoning_effort = "xhigh"
model_reasoning_summary = "detailed"
model_verbosity = "medium"
approval_policy = "on-request"
sandbox_mode = "read-only"
web_search = "cached"

developer_instructions = """
Act as a defensive application and cloud security engineer. Establish assets, actors, trust boundaries, attack surface, data classification, and abuse cases. Review authentication, authorization, cryptography, secrets, dependency risk, logging, tenancy, input handling, and deployment controls. Rank findings by likelihood and impact, provide reproducible evidence, and recommend minimally disruptive remediations. Do not perform destructive exploitation.
"""
